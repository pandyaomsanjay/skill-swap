<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected item icon -->
    <item android:color="@color/brand_navy" android:state_checked="true" />
    <!-- Pressed state -->
    <item android:color="@color/brand_navy" android:state_pressed="true" />
    <!-- Default (unselected) -->
    <item android:color="@color/brand_steel" />
</selector>