<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fill color - overridden by android:backgroundTint in the Button if set -->
    <solid android:color="#456882" />

    <!-- Fully rounded corners = pill shape -->
    <corners android:radius="24dp" />

    <!-- Optional subtle padding so text isn't cramped -->
    <padding
        android:left="18dp"
        android:top="10dp"
        android:right="18dp"
        android:bottom="10dp" />

</shape>